#!/usr/bin/env sh

# Ensure CONFIG_DIR exists
: "${CONFIG_DIR:=$HOME/.config/sketchybar}"
PLUGIN_DIR="$CONFIG_DIR/plugins"
THEME_FILE="$HOME/.config/themes/current/shellcolors.sh"

# Theme Colors
if [ -r "$THEME_FILE" ]; then
  source "$THEME_FILE"
else
  BACKGROUND=0xbb111111
  CLOCK=0xffffffff
  ACTIVE=0xfffe8019
fi

##### Bar Appearance #####
bar=(
  position=top
  height=26
  margin=0
  y_offset=0
  blur_radius=10
  color=0xbb111111 # Darker, solid background
  corner_radius=0
  border_width=0
  border_color=0x44ffffff
  shadow=off
)
sketchybar --bar "${bar[@]}"

##### Defaults #####
FONT="JetBrainsMonoNL Nerd Font"

default=(
  icon.font="$FONT:Bold:12.0" # Slightly smaller
  label.font="$FONT:Bold:12.0" # Slightly smaller
  icon.color=0xffebdbb2 # Gruvbox light FG
  label.color=0xffebdbb2
  icon.padding_left=5
  icon.padding_right=5
  label.padding_left=5
  label.padding_right=5
  background.height=20 # Slimmer
  background.corner_radius=3
)
sketchybar --default "${default[@]}"

##### Spaces (Dynamic from Yabai) #####
sketchybar --add event window_change
sketchybar --add event space_windows_change

# Initial load of spaces
"$PLUGIN_DIR/space_refresh.sh"

# Subscribe to events to refresh spaces dynamically
sketchybar --add item space_observer left \
           --set space_observer drawing=off \
                                script="$PLUGIN_DIR/space_refresh.sh" \
           --subscribe space_observer space_change space_windows_change

##### Items #####

# Media: Increased padding to center between spaces and front_app
# Start listener if not running
pgrep -f "media.sh listen" >/dev/null || "$PLUGIN_DIR/media.sh" listen &

sketchybar --add item media left \
           --set media icon="󰝚" \
                       label.max_chars=20 \
                       scroll_texts=off \
                       update_freq=2 \
                       padding_left=15 \
                       script="$PLUGIN_DIR/media.sh" \
                       icon.font="JetBrainsMonoNL Nerd Font:Bold:13.0" \
                       background.color=$BACKGROUND \
                       background.drawing=on \
           --subscribe media media_change

# Front App (Workspace Name)
sketchybar --add item front_app center \
           --set front_app script="$PLUGIN_DIR/front_app.sh" \
                           icon.font="sketchybar-app-font:Regular:14.0" \
                           background.drawing=off \
           --subscribe front_app front_app_switched

# System Items (Right side)
sketchybar --add item clock right \
           --set clock update_freq=10 \
                       icon="󰥔" \
                       script="$PLUGIN_DIR/clock.sh" \
                       background.color=$BACKGROUND \
                       background.drawing=on

sketchybar --add item volume right \
           --set volume script="$PLUGIN_DIR/volume.sh" \
                       background.color=$BACKGROUND \
                       background.drawing=on \
           --subscribe volume volume_change

sketchybar --add item battery right \
           --set battery update_freq=60 \
                         script="$PLUGIN_DIR/battery.sh" \
                         background.color=$BACKGROUND \
                         background.drawing=on \
           --subscribe battery system_woke power_source_change

sketchybar --update
