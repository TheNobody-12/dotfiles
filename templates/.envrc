# Direnv + uv auto-activation template
# Place this in a project root and run `direnv allow`

# Activate uv-managed venv if it exists
if [ -d ".venv" ]; then
  source .venv/bin/activate
fi

# Or use nix-direnv to load a per-project flake:
# use flake
